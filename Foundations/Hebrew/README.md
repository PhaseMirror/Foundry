# Foundations – Hebrew Formalization Test Harness

This directory contains a **stand‑alone test harness** that exercises the Lean and Rust formalizations created under the main `Hebrew` workspace.

## Contents
- `run_all.sh` – Shell script that builds the Lean project and runs the Rust/Kani verification suite.
- `README.md` – This file, describing the purpose and usage of the harness.

## How to Use
```bash
# From this directory
./run_all.sh
```
The script will:
1. Navigate to the original project at `../../Hebrew`.
2. Run `lake build` to compile the Lean modules.
3. Run `cargo kani` to verify the Rust code.
4. Report success or failure for each step.

## Prerequisites
- **Lean** – installed via `elan` (the script sets up the PATH).
- **Rust** – stable toolchain with `cargo`.
- **Kani** – installed (`cargo install kani`).

Both the Lean and Rust parts are already configured in the parent repository; this harness simply automates the common workflow for CI or local testing.

---

### Test Harness Script (`run_all.sh`)
```bash
#!/usr/bin/env bash
set -euo pipefail

# Resolve the path to the main Hebrew project (two levels up)
PROJECT_ROOT=$(realpath ../../Hebrew)

echo "=== Building Lean project ==="
pushd "$PROJECT_ROOT/lean" > /dev/null
# Ensure elan is on PATH (if not already)
if ! command -v lake &> /dev/null; then
  echo "Lake not found – installing elan…"
  curl -L https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | bash -s -- --default-toolchain stable
  export PATH="$HOME/.elan/bin:$PATH"
fi
lake build
popd > /dev/null

echo "=== Verifying Rust with Kani ==="
pushd "$PROJECT_ROOT/rust" > /dev/null
# Install Kani if missing
if ! command -v cargo-kani &> /dev/null; then
  cargo install kani --locked
fi
cargo kani
popd > /dev/null

echo "All checks passed!"
```

Make the script executable:
```bash
chmod +x run_all.sh
```

Now you can run `./run_all.sh` to verify the whole formalization stack.

---

### Documentation
All naming conventions, directory layout, proof style guidelines, and CI configuration are described in the top‑level `README.md` located at the root of the repository (`/home/citizen/Multiplicity/Hebrew/README.md`). This harness simply re‑uses those conventions.

---

*Generated by Antigravity, the advanced agentic coding assistant.*
