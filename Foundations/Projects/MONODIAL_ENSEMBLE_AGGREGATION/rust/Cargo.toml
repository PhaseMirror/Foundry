[package]
name = "monodial_ensemble_aggregation"
version = "0.1.0"
edition = "2024"

[dependencies]
# No external runtime dependencies for the core logic

[dev-dependencies]
# Kani for verification
kani = "0.67"
