[package]
name = "zeta_phi_pi"
version = "0.1.0"
edition = "2024"

[dependencies]
# No external dependencies needed for the simple example

[dev-dependencies]
# Kani dependencies for verification
# kani = { path = "/tmp/kani_repo/kani/library/kani" }
