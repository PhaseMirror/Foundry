[package]
name = "automorphic_learning"
version = "0.1.0"
edition = "2021"

[dependencies]
nalgebra = "0.33"
num-bigint = "0.4"
num-traits = "0.2"

[dev-dependencies]
kani = { git = "https://github.com/model-checking/kani", tag = "kani-0.67.0" }

# Prevent cargo from treating this crate as part of a parent workspace
[workspace]
members = []
