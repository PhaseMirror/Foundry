[package]
name = "aztftc"
version = "0.1.0"
edition = "2021"

[dependencies]

[dev-dependencies]
kani = { version = "0.67.0", features = ["std"] }

# Prevent cargo from treating this crate as part of a parent workspace
[workspace]
members = []
