[package]
name = "certificate-runtime"
version = "0.1.0"
edition = "2024"
description = "Certified graph learning runtime — production-grade spectral contraction certificate checker"

[lib]
crate-type = ["lib", "staticlib", "cdylib"]

[dependencies]
thiserror = "2"

[features]
lean-ffi = []

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ["cfg(kani)"] }
