# universal_closure.yaml — Associator Defect Tolerances (Δ ≤ ε)
# ADR-231 §3: Declarative boundary contracts for Universal Closure Theory.
# Generated by generate_certificates.py from Kani verification logs.

contract_id: universal_closure
version: "1.0.0"
created_at: "2026-07-23T00:00:00Z"
governance:
  owner: the-examiner
  governor: the-examiner
  fail_closed: true
  sig_gov_kill: true

# Associator defect tolerance: Δ ≤ ε
associator:
  defect_tolerance: "1e-3"
  strict_inequality: true
  dimension_limit: 12

# Closure operator bounds
closure:
  lip_bound: "1/1"
  idempotence_verified: true
  monotonicity_verified: true
  union_preservation_verified: true

# Universal closure algebra parameters
universal_closure:
  max_set_size: 100
  max_closure_depth: 5
  epsilon: "1e-3"

# Operator composition bounds for closure operators
operators:
  - name: closure_op_discrete
    type: ClosureOp
    lip_constant: "0"
    verified: true
    kani_harness: verify_trace_bounds

  - name: closure_op_topological
    type: ClosureOp
    lip_constant: "1/1"
    verified: true
    kani_harness: verify_rank_consistent

  - name: associator_defect_bound
    type: DefectBound
    lip_constant: "1/1000"
    verified: true
    kani_harness: verify_coherence_finite_primes

# Rational interval FPU bypass
arithmetic:
  representation: RatInterval
  exact_rational: true
  float_bypass: true
  hardware_dependent_rounding: false

# Cross-language parity
parity:
  lean_module: Core.Foundations.UniversalClosure
  rust_module: pirtm_engine.ofa
  byte_exact: true
  check_interval: "1h"

# Audit
audit:
  poseidon2_commitment: true
  pweh_enabled: true
  log_retention: "7y"