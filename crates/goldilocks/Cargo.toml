[package]
name = "goldilocks"
version = "1.0.0-alpha"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
rand = "0.8"
criterion = "0.5"

[[bench]]
name = "field_ops"
harness = false
