[package]
name = "harness"
version = "1.0.0-alpha"
edition = "2021"

[dependencies]
anyhow = "1.0"
sha2 = "0.10"
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ndarray = "0.15"
chrono = { version = "0.4", features = ["serde"] }
