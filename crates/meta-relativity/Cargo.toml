[package]
name = "meta-relativity"
version = "1.0.0-alpha"
edition = "2021"

[dependencies]
nalgebra = "0.33"
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
thiserror = "2.0.18"

[dev-dependencies]
kani = "0.0.1"

[dependencies.archivum]
path = "../archivum"
optional = true

[features]
archivum = ["dep:archivum"]
triple-lock = ["archivum"]
