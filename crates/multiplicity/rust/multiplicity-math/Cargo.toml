[package]
name = "multiplicity-math"
version = "1.0.0-alpha"
authors = ["Multiplicity Foundation"]
edition = "2021"
license = "MIT"

[dependencies]
multiplicity-common = { path = "../../../../substrates/commons" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
thiserror = "1.0"
tracing = "0.1"
schemars = "0.8"
num-traits = "0.2"
num-complex = "0.4"

[dev-dependencies]
