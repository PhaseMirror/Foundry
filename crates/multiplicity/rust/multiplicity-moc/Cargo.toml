[package]
name = "multiplicity-moc"
version = "1.0.0-alpha"
authors = ["Multiplicity Foundation"]
edition = "2021"
license = "MIT"

[dependencies]
multiplicity-common = { path = "../../../../substrates/commons" }
multiplicity-math = { path = "../multiplicity-math" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
thiserror = "1.0"
tracing = "0.1"
schemars = "0.8"
nalgebra = "0.32"

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
