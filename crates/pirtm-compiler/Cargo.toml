[package]
name = "pirtm-compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
tree-sitter = "0.20"
tree-sitter-c = "0.20"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
archivum = { path = "../archivum" }

[dev-dependencies]

[build-dependencies]
cc = "1.0"
