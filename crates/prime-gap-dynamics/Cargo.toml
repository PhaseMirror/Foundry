[package]
name = "rust-prime-gap-dynamics"
version = "1.0.0-alpha"
edition = "2021"

[dependencies]
ndarray = "0.15"
num-complex = "0.4"
serde = { version = "1.0", features = ["derive"] }
# For time evolution/ODE solving
# ndarray-ode-solvers might be useful, or a custom one
