[package]
name = "mirror-dissonance"
version = "1.0.0-alpha"
edition = "2021"
description = "≡-Compiler Core for Multiplicity Operator Calculus (MOC)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
lazy_static = "1.4"
