[package]
name = "resolvent-verify"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "resolvent_verify"
path = "src/main.rs"

# NOTE: `kani` is NOT a normal dependency. `cargo kani` provides the `kani`
# crate from its own verification sysroot. The proofs live in the
# `#[cfg(kani)]` module below and are excluded from ordinary `cargo build`.
