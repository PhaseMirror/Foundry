[package]
name = "operators_hybrid"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# Kani verifier dependencies are managed via cargo-kani when running tests.
