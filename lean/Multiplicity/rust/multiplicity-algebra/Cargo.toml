[package]
name = "multiplicity-algebra"
version.workspace = true
edition.workspace = true
description = "Multiplicity kernel (ADR-0001): gcd/lcm lattice and polynomial algebra on u64/i64"
license = "Apache-2.0"

[dependencies]
multiplicity-core = { path = "../multiplicity-core" }

[dev-dependencies]
serde_json = "1.0"
