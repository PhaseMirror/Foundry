[package]
name = "multiplicity-core"
version.workspace = true
edition.workspace = true
description = "Multiplicity kernel (ADR-0001): core total primitives on u64/i64"
license = "Apache-2.0"

[dependencies]

[dev-dependencies]
serde_json = "1.0"
