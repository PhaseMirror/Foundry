[package]
name = "multiplicity-primes"
version.workspace = true
edition.workspace = true
description = "Multiplicity kernel (ADR-0001): primes, valuation, factorization on u64"
license = "Apache-2.0"

[dependencies]

[dev-dependencies]
serde_json = "1.0"
