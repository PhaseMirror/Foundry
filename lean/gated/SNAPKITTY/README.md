# SnapKitty / Lean 4 Integration

This directory contains the formal Lean 4 binding and verification artifacts for the **SnapKitty Sovereign OS Math Engine**.

## Overview

SnapKitty implements a sovereign mathematics core, encompassing:
- Entropy tracking (Shannon, Von Neumann)
- Quantum monad representations
- Thermodynamic window bounds (Thermal EMA)
- Verdict algebra and borrow-chain validation
- 5-pass ERE scoring matrices

## Sedona Spine Compliance

Per the **Consciousness-First Protocol (CFP)** and the Sedona Spine mandate:
1. **ALP Policy Subordination:** The SnapKitty decision matrix (`Verdict`) is fully subordinate to the Multiplicity `ALP` (Axiomatic Law Policy) engine.
2. **Bridge Verification:** The file `SnapKitty/SedonaSpine.lean` enforces that even if SnapKitty approves an execution path (e.g., `ERE-THRESHOLD-MET`), the transition *must* additionally clear the `ALP.PolicyEngine` rules under `TrustLevel.Internal`.
3. **Immutability:** SnapKitty operates purely within the bounds of `MOC` resonance certificates. It cannot bypass the Phase Mirror circuit breakers.

See `SnapKitty/SedonaSpine.lean` for the explicit cross-engine binding.
