#!/bin/sh
# If git-lfs is available, delegate to it; otherwise warn and continue.
if command -v git-lfs >/dev/null 2>&1; then
	git lfs post-commit "$@"
else
	echo "⚠️  git-lfs not found; skipping git-lfs post-commit hook"
fi
