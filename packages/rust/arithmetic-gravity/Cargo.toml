[package]
name = "arithmetic-gravity"
version = "0.1.0"
edition = "2021"

[dependencies]
num-rational = "0.4"
num-traits = "0.2"

[dev-dependencies]
kani = "0.58"   # Kani verifier; version may vary; use latest stable

[workspace]
