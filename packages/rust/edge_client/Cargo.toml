[package]
name = "edge_client"
version = "0.1.0"
edition = "2021"

[dependencies]
materia_commons = { path = "../materia_commons" }
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { workspace = true }
