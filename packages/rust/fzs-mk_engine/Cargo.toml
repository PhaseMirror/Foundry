[package]
name = "kernel"
version = "1.0.0-alpha"
edition = "2024"

[dependencies]
ndarray = "0.16"
num-complex = "0.4"
# Using statrs for potential statistical needs if necessary later
statrs = "0.17"
