[package]
name = "pyjson"
version = "0.1.0"
edition = "2021"
description = "Python-json-compatible serialization (default-separator and compact sort_keys formats) for Rust ports of the Python bridge packages"
license = "Prime Materia Open Commons and Bound Works License v1.0"
repository = "https://github.com/PhaseMirror/Prime"

[lib]
name = "pyjson"
path = "src/lib.rs"

[dependencies]
serde_json = { workspace = true }

[dev-dependencies]