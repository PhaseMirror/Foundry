[package]
name = "wordlove-ffi"
version = "0.1.0"
edition = "2021"
description = "Rust binding to the Lean WordLove hybrid-primality and certified-coupling C ABI (ADR-0031 §6, ADR-0033 P5 Realization)"

[lib]
crate-type = ["rlib"]

[dependencies]
lean-rs = "0.7"

[build-dependencies]
lean-toolchain = "0.7"
