- id: lever-coding-session
  server_binding: opencode
  tool: coding.plan
  trust: internal
  description: "Generate governed refactor plans and patches for Commander."
  constraints:
    - no direct filesystem writes
    - no git writes
    - no shell execution
    - all edits must pass ALP
